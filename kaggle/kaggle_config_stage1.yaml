# Kaggle股票趋势预测比赛配置文件 - 阶段1: 快速验证
# 用于快速验证数据流程和模型加载

data:
  data_path: "/mnt/shared-storage-user/zhaoliangliang/code/Kronos/kaggle_data/train_combined.csv"
  lookback_window: 256
  predict_window: 1
  max_context: 512
  clip: 5.0
  train_ratio: 0.9
  val_ratio: 0.1
  test_ratio: 0.0

training:
  # 快速验证：减少训练轮数
  tokenizer_epochs: 10      # 从20减少到10
  basemodel_epochs: 8       # 从15减少到8
  batch_size: 32
  log_interval: 50
  num_workers: 4
  seed: 42
  
  tokenizer_learning_rate: 0.0002
  predictor_learning_rate: 0.00004
  
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_weight_decay: 0.1
  accumulation_steps: 1

model_paths:
  pretrained_tokenizer: "/mnt/shared-storage-user/zhaoliangliang/model/Kronos-Tokenizer-base"
  pretrained_predictor: "/mnt/shared-storage-user/zhaoliangliang/model/Kronos-base"
  exp_name: "kaggle_stock_trends_stage1"
  base_path: "/mnt/shared-storage-user/zhaoliangliang/code/Kronos/kaggle_finetuned/"
  base_save_path: ""
  finetuned_tokenizer: ""
  tokenizer_save_name: "tokenizer"
  basemodel_save_name: "basemodel"

experiment:
  name: "kaggle_stock_trends_finetune_stage1"
  description: "快速验证：Kronos for Kaggle stock trends prediction"
  use_comet: false
  train_tokenizer: true
  train_basemodel: true
  skip_existing: false

device:
  use_cuda: true
  device_id: 0

